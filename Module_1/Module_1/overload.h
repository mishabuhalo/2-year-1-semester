#pragma once
#include "math.h"
#include <iostream>
#include <string>
#include <list>
using namespace std;

	int f(int n);
	int f(double d);
	int f(string s);
	int f(pair <int, int> p);
	template <typename T>
	int f(T a);

